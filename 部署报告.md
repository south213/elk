# ELK实战项目 - K8s 部署报告

**部署日期**: 2026-05-13  
**部署环境**: Docker Desktop Kubernetes (v1.34.3)  
**集群节点**: desktop-control-plane (1 节点)

---

## 一、部署结果

| 组件 | 类型 | 副本数 | 状态 |
|------|------|:--:|:--:|
| postgres | Deployment | 1 | Running |
| redis | Deployment | 1 | Running |
| elasticsearch | Deployment | 1 | Running |
| logstash | Deployment | 1 | Running |
| filebeat | DaemonSet | 1 | Running |
| kibana | Deployment | 1 | Running |
| backend（Flask API） | Deployment | 3 | Running |
| frontend（React + Nginx） | Deployment | 2 | Running |

**总计**: 8 个服务，12 个 Pod，全部 Running

---

## 二、访问方式

| 服务 | 地址 | 备注 |
|------|------|------|
| 前端 | http://localhost:8080 | port-forward |
| Kibana | http://localhost:5601 | port-forward |

---

## 三、部署过程中遇到的问题与解决

### 1. 阿里云公共镜像拉取失败
- **问题**: `registry.aliyuncs.com` 个人账号无法拉取，ImagePullBackOff
- **解决**: 替换为 `docker.m.daocloud.io` 代理镜像

### 2. Flask 后端 CrashLoopBackOff
- **问题**: Flask 2.0.1 与 Werkzeug 3.x 不兼容，`url_quote` 导入失败
- **解决**: `requirements.txt` 增加 `Werkzeug==2.3.7` 锁定版本，重新构建推送镜像

### 3. Elasticsearch Service 创建失败
- **问题**: Service 端口缺少 `name` 字段（K8s 1.33+ 强制要求）
- **解决**: 添加 `name: http` 和 `name: transport`

### 4. Ingress 域名访问 502
- **问题**: `ingressClassName` 未设置，Ingress Controller 忽略该规则
- **解决**: 添加 `ingressClassName: nginx`
- **残留问题**: 设置后仍 502，根因是 cloud 版 Ingress Controller 的 LoadBalancer 在 Docker Desktop 下兼容性问题。生产环境上云后 ACK 自带 Ingress Controller，不会遇到

### 5. 配置缺失补充
- `logstash-deployment.yaml`: 补充 `logstash-config` ConfigMap（原独立 `logstash.conf` 内容）
- `kibana-deployment.yaml`: 补充 `kibana-config` ConfigMap（原独立 `kibana.yml` 内容）

---

## 四、修改的文件清单

| 文件 | 改动内容 |
|------|------|
| `services/backend/Dockerfile` | 基础镜像改为 `docker.m.daocloud.io/library/python:3.9-slim` |
| `services/frontend/Dockerfile` | 基础镜像改为 `docker.m.daocloud.io/library/nginx:alpine` |
| `services/backend/requirements.txt` | 新增 `Werkzeug==2.3.7` |
| `configs/backend-deployment.yaml` | image 改为 ACR 地址 |
| `configs/frontend-deployment.yaml` | image 改为 ACR 地址 |
| `configs/elasticsearch-deployment.yaml` | 镜像源 + Service 端口 name |
| `configs/logstash-deployment.yaml` | 镜像源 + 补充 ConfigMap |
| `configs/kibana-deployment.yaml` | 镜像源 + 补充 ConfigMap |
| `configs/filebeat-deployment.yaml` | 镜像源 |
| `configs/postgres-deployment.yaml` | 镜像源 |
| `configs/redis-deployment.yaml` | 镜像源 |
| `configs/ingress.yaml` | 添加 `ingressClassName: nginx`，移除 rewrite-target |
| `configs/logstash.conf` | 已删除（内容整合到 ConfigMap） |
| `configs/filebeat.yml` | 已删除（内容整合到 ConfigMap） |
| `configs/kibana.yml` | 已删除（内容整合到 ConfigMap） |

---

## 五、镜像构建推送记录

| 镜像 | ACR 地址 | 大小 |
|------|------|:--:|
| backend | `crpi-yd9kld5exm3ui8a0.cn-guangzhou.personal.cr.aliyuncs.com/my_elk/backend:latest` | 216MB |
| frontend | `crpi-yd9kld5exm3ui8a0.cn-guangzhou.personal.cr.aliyuncs.com/my_elk/frontend:latest` | 92.6MB |

---

## 六、健康检查

```
后端 /health 端点返回: {"status":"healthy","timestamp":"2026-05-13T10:14:03.924335"}
```

## 七、环境清理清单

- [ ] 删除 task-management namespace
- [ ] 删除 ingress-nginx namespace
- [ ] 停止 port-forward 进程
- [ ] 可选：关闭 Docker Desktop Kubernetes
